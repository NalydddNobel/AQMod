using Terraria.ModLoader;

namespace Aequus.NPCs.BossMonsters.YinYang {
    public class Yin : ModNPC {
        public override bool IsLoadingEnabled(Mod mod) {
            return false;
        }
    }

    public class Yang : ModNPC {
        public override bool IsLoadingEnabled(Mod mod) {
            return false;
        }
    }
}