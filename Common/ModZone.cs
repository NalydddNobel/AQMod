namespace Aequus.Common;

/// <summary>
/// A <see cref="ModBiome"/> with special properties to instead act as a "zone" rather than a proper biome.
/// </summary>
public abstract class ModZone : ModBiome {
}